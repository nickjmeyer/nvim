return {
  {
    "dhruvasagar/vim-table-mode",
    -- Optional: lazy-load on filetype markdown or when the command is used
    ft = "markdown", -- This loads the plugin for markdown files
    cmd = "TableModelToggle", -- This loads the plugin when you run :TableModelToggle
  },
}
